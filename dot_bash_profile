# .bash_profile

# Get the aliases and functions
if [ -f ~/.bashrc ]; then
	. ~/.bashrc
fi

# User specific environment and startup programs
test -r /home/luna/.opam/opam-init/init.sh && . /home/luna/.opam/opam-init/init.sh > /dev/null 2> /dev/null || true
